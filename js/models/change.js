define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var Change = Backbone.Model.extend({
    
	defaults: {
    },
    
    initialize: function(){
    }
    
  });
  return Change;

});
